$(document).ready(function() {
  $('#fullpage').fullpage({
    sectionsColor: ['#ffffff', '#000000'],
    anchors: ['about', 'work', 'contact'],
    menu: '#menu',
    scrollOverflow: true,
    resetSliders: true,
  });
});
